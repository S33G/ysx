{
  "name": "ysx-core",
  "version": "1.0.0",
  "description": "Core parser and transpiler for YSX (YAML Syntax Extension) for React",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@babel/generator": "^7.26.0",
    "@babel/template": "^7.26.0",
    "@babel/types": "^7.26.0",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
